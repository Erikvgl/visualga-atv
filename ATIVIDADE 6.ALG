algoritmo "atividade 6"

var
 pao, bolo, paoq, boloq, total:real
inicio
 escreva("quantidade de pães:", pao)
 leia(pao)
 escreva("quantidade de bolo:", bolo)
 leia(bolo)
 
 paoq <- pao * 0,35
 boloq <- bolo * 5
 total <- paoq+boloq
 
 escreval("sua compra deu um total de:", total)
fimalgoritmo
